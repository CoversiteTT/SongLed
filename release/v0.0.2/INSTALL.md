# 安装

1. 解压并运行 `SongLed_v0.0.2_flasher.zip` 内 `SongLedFlasher.exe`。
2. 连接 ESP32-S3（COM）。
3. 选择 `songled_v0.0.2_merged.bin`。
4. 点击 Start Flashing。

说明：单文件烧录，地址固定 `0x0`。

PC：
- 轻量版：`pc-lite/SongLedPcLite.exe`
- 完整版：`pc-full/SongLedPc.exe`
